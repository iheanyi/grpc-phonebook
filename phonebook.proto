syntax = "proto3";

package grpc_phonebook;

message Contact {
  string name = 1;
  string email = 2;

  repeated PhoneNumber phone_number = 3;
}

message PhoneNumber {
  enum PhoneType {
    MOBILE = 0;
    HOME = 1;
    WORK = 2;
  }

  string number = 1;
  PhoneType type = 2;
}
